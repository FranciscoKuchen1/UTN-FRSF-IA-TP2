# LLM
google-genai>=0.1.0
groq>=0.5.0

# RAG y embeddings
supabase>=2.4.0
pgvector>=0.2.0

PyMuPDF>=1.24.0        # parse_pdf: extracción de texto desde PDFs
python-docx>=1.1.0     # parse_word: extracción de texto desde archivos .docx
nomic>=3.9.0


# Memoria (opcional: solo necesario si MEMORY_BACKEND=redis en .env)
redis>=5.0.0

# Backend
fastapi>=0.111.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# Observabilidad
langfuse>=2.0.0

# Agente / tools
requests>=2.32.0
httpx>=0.27.0

# Auth — la validación del JWT se delega completamente a Supabase Auth
# a través del cliente supabase-py; no se usa python-jose ni PyJWT
python-multipart>=0.0.9  # necesario para que FastAPI procese form-data en OAuth flows
